#include "point.h"

/*
template <int N>
Point<N>::Point()//:x(0),y(0)
{
    for(int i=0;i<N;i++)
        board[i]=0;
}


template <int N>
Point<N>::Point(int number,...)
{
    va_list arguments;
        board[0]=number;
        va_start( arguments, number );
        for( int i = 1; i < number; i++ )
             board[i]=va_arg(arguments,double);
     va_end( arguments );

}









template <int N>
double Point<N>::getValue(int i)
{
    return board[i];
}

template <int N>
void Point<N>::setValue(int i,double value)
{
    this->board[i]=value;
}
*/
